Items incomplete are
Initiate an RDS instance (db.t2.micro class with PostgreSQL) in a private subnet.
• automate the installation of PostgreSQL client in web servers.
-AutoScaling of Bastion Server
BONUS Features:
Set up a Cloud Front distribution to speed up the distribution of static content stored in
the S3 bucket you created before.
• Set up an SNS topic to notify through email whenever a web server is launched and
terminated. The email address should be received by a CloudFormation parameter.